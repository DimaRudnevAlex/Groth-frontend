import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    root: {
        boxShadow: 'none',
    },
    toolbar: {
        justifyContent: 'space-between',
        padding: '24px 20px',
    },
    menuIcon: {
        marginRight: '10px',
        cursor: 'pointer',
    },
});

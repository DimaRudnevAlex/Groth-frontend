import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    searchBlock: {
        display: 'flex',
        maxHeight: '48px',
        borderRadius: '8px',
    },
    searchInput: {
        padding: '18px 12px',
    },
});

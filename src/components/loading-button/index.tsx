import { styled } from '@mui/styles';
import { Button } from '@mui/material';

const AppLoadingButton = styled(Button)({
    borderRadius: '4px',
    background: '#1900D5 !important',
    boxShadow: '0 1px 7px #332a76 !important',
    padding: '10px 20px !important',
    width: '100%',
});

export default AppLoadingButton;

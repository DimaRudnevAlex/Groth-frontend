import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => {
    return {
        navBlock: {
            width: '100%',
            display: 'flex',
            alignItems: 'center',
            gap: '10px',
            padding: '30px 15px',
        },
    };
});
